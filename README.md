### todo application

handy application that tracks your todo's 
psql db for persistent tasks


## how?

1. create `.env` with `DATABASE_URL` pointing to your db.
2. run db/migrate.rb and db/add_position.rb to create tables.
3. start get organised.

## standalone?

1. once on localhost:3212. click install in the address bar in chrome.
